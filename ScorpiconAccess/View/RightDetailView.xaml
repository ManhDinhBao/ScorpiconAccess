<Window x:Class="ScorpiconAccess.View.RightDetailView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScorpiconAccess.View"
        mc:Ignorable="d"
        Title="Right Detail" Height="380" Width="300" ResizeMode="NoResize" Loaded="Window_Loaded">
    <StackPanel Orientation="Vertical">
        <ListBox x:Name="lbRightDetail" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="269" Margin="10,20,10,0" Width="250">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <ListBoxItem IsSelected="{Binding IsChecked}" >
                        <CheckBox IsChecked="{Binding IsChecked}" Content="{Binding Path=TextBinding}" />
                    </ListBoxItem>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="btSave" Click="btSave_Click" Height="30" Width="70" HorizontalAlignment="Right" Margin="0,20,22,10">
            <StackPanel Orientation="Horizontal">
                <Image Width="15" Height="15" Source="..\Icon\save_gray_30px.png"/>
                <Label Content="Save" FontFamily="Consolas" FontSize="9"/>
            </StackPanel>
        </Button>
    </StackPanel>
</Window>
